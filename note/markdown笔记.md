## markdown笔记

## 标题

使用#和一个空格，一至六级分别用几个#

```markdown
# 标题
```

 效果:

![屏幕截图 2026-02-01 210821](https://dyins-storage-01.oss-cn-guangzhou.aliyuncs.com/202602012357724.png)

```markdown
[跳转到标题部分](# 标题)
```

[跳转到标题部分](# 标题)

## 换行

### 段落间换行：

段落的换行是使用两个以上空格加上回车。

或者：

一：直接enter：

第一行

第二行

二：shift+enter：

第一行
第二行

### 非段落间换行

一：

```markdown
第一行内容（这里有两个空格）  
第二行内容
```

第一行内容（这里有两个空格）  
第二行内容

## 加粗

```markdown
**加粗**
```

效果：**加粗**

## 斜体

```markdown
*斜体*
```

效果：*斜体*

## 粗斜体

```markdown
***粗斜体***
```

效果：***粗斜体***

## 分割线

```markdown
***

---
```

效果：

****



---

## 删除线

两端加上~~

```markdown
~~这是删除线~~
```

效果：~~这是删除线~~

## 下划线

```markdown
<u>下划线</u>
```

效果：<u>下划线</u>

## 脚注

格式：

```markdown
[^要注明的文本]
```

用法：(注意符号要是英文模式下的)

```markdown
这是[^脚注]

[^脚注]: 脚注是对文本的补充说明
```

这是[^脚注]

[^脚注]:脚注是对文本的补充说明

如图：

<img src="https://dyins-storage-01.oss-cn-guangzhou.aliyuncs.com/202602020000933.png" style="zoom:50%;float:left" />

## 行内代码标记

使用一个反引号`` ` ``

```markdown
`code`
`` `code` ``
```

效果：`code`

​	  `` `code` ``

## 高亮

```markdown
这是==高亮文本==
这是<mark>高亮文本</mark>
```

这是==高亮文本==
这是<mark>高亮文本</mark>

## 列表

### 无序列表

使用一个星号* (或者一个减号 - ) 和一个空格：

```markdown
* 第一项
* 第二项
* 第三项
```

* 第一项
* 第二项
* 第三项

### 有序列表：

```markdown
1. 第一项
2. 第二项
4. 第三项
```

实际：（自动修正）

1. 第一项
2. 第二项
3. 第三项

### 嵌套使用：

列表混合嵌套只需在子列表中的选项前面添加两个或四个空格即可

```markdown
1. 第一项：
    - 第一项嵌套的第一个元素
    - 第一项嵌套的第二个元素
2. 第二项：
    - 第二项嵌套的第一个元素
    - 第二项嵌套的第二个元素
```

1. 第一项：
    - 第一项嵌套的第一个元素
    - 第一项嵌套的第二个元素
2. 第二项：
    - 第二项嵌套的第一个元素
    - 第二项嵌套的第二个元素

### 任务列表：

***（注意空格）***

```markdown
- [ ] 未完成的任务
- [x] 已完成的任务
- [ ] 另一个未完成的任务

```

效果：

- [ ] 未完成的任务
- [x] 已完成的任务
- [ ] 另一个未完成的任务

## 引用块

段落开头使用 > 后面加一个空格：

> 这是一个引用

## 代码

你也可以用 **```** 包裹一段代码，并指定一种语言（也可以不指定）

````markdown
```markdown
这是markdown代码
```
````

效果：

```markdown
这是markdown代码
```

## 链接

```markdown
这是一个链接 [菜鸟教程](https://www.runoob.com)
```

这是一个链接 [菜鸟教程](https://www.runoob.com)

## 图片

```markdown
<!-- 居中对齐 -->
<div align="center">
  <img src="image.jpg" alt="居中图片" width="400">
</div>

<!-- 左对齐（默认） -->
<img src="image.jpg" alt="左对齐图片" style="float: left; margin-right: 20px;">

<!-- 右对齐 -->
<img src="image.jpg" alt="右对齐图片" style="float: right; margin-left: 20px;">
```

 







